import { ChangeOutletDirective } from './change-outlet.directive';

describe('ChangeOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeOutletDirective();
    expect(directive).toBeTruthy();
  });
});
